shoppingList = ["milk", "eggs", "bread", "apples", "coffee", "biscuits"]

//iterations using for loop
//for (condition saying for each item in array follow rule)

// for (item of shoppingList) {
//     console.log(item);
// }

// for loop with greater control, for (make a counter, limit counter-set when it will stop, how it modifies after each iteration)
for (index = 0; index < shoppingList.length; index++) {
    console.log(shoppingList[index]);
   
}